module.exports = {
  root: true,
  env: {
    browser: true,
    es2021: true,
    node: true,
  },
  parserOptions: {
    ecmaVersion: 'latest',
    sourceType: 'module',
  },
  extends: [
    // You can extend Airbnb or other configs if you install them.
  ],
  rules: {
    // Custom linting rules can be placed here. Since this project does not
    // install eslint or plugins, this file serves as documentation only.
  },
};
